This is a simplified web crawler for fetching texts, images, etc on a website.
Currently, it can only fetch text and links.
Image support and turning it into an NPM package are in the works, as well as exporting the main function itself for easier use.